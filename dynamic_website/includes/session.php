<?php
// Start the session
session_start();

/*
// Set session variables
$_SESSION['username'] = 'JohnDoe';
$_SESSION['email'] = 'johndoe@example.com';

echo "Session variables are set.<br>";
echo "Username: " . $_SESSION['username'] . "<br>";
echo "Email: " . $_SESSION['email'] . "<br>";

// To remove a specific session variable
unset($_SESSION['username']); // Removes only the 'username' session variable
echo "Username session variable is removed.<br>";

// Check if the 'username' session variable is unset
if (!isset($_SESSION['username'])) {
    echo "Username session variable does not exist.<br>";
}

// To remove all session variables but keep the session active
session_unset(); // Clears all session variables
echo "All session variables are removed.<br>";

// To completely destroy the session (including the session ID)
session_destroy();
echo "The session is destroyed.";
*/
?>
